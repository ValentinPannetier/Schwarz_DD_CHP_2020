#include <fstream>
#include <iostream>
#include <string>

void charge (int, int, int, int &, int &);

void partitions (int, int, int, int *);

void build_meshes (std::string);

bool compare_string (std::string, std::string);

void read_mesh (int, int, int *, int *, int *);
